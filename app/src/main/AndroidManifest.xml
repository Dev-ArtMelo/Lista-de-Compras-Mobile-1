<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.unaerp.listadecompras">

    <application
        android:allowBackup="true"
        android:label="Lista de Compras App"
        android:supportsRtl="true"
        android:theme="@style/Theme.App.Light">
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.login.CadastroActivity" />
        <activity android:name=".ui.list.ListaDeComprasActivity" />
        <activity android:name=".ui.list.NovaListaActivity" />
        <activity android:name=".ui.item.ItensActivity" />
        <activity android:name=".ui.item.NovoItemActivity" />
    </application>

</manifest>
